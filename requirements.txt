# ============================================================================
# PRODUCTION REQUIREMENTS - Optimized for Render Free Tier (512MB RAM)
# ============================================================================

# Force CPU-only PyTorch (prevent 4GB GPU dependencies)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1; python_version < "3.13"
torch==2.11.0+cpu; python_version >= "3.13"

# Core RAG dependencies
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
qdrant-client>=1.7.0
numpy>=1.26.0,<2.5.0

# LLM / Groq
groq>=0.5.0

# Async HTTP & Connection Pooling
aiohttp>=3.9.0
httpx>=0.27.0

# Advanced Retrieval
rank-bm25>=0.2.2

# Caching
aiocache>=0.12.0

# Streamlit UI
streamlit>=1.32.0

# OCR (optional - requires system Tesseract)
pytesseract>=0.3.10
Pillow>=10.2.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# ============================================================================
# EXCLUDED FROM PRODUCTION (not needed at runtime):
# - datasets (only for data collection)
# - huggingface-hub (pulled by sentence-transformers)
# - praw (Reddit collection - dev only)
# - pandas (not used in runtime)
# ============================================================================
