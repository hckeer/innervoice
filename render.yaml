services:
  - type: web
    name: innervoice
    runtime: python
    plan: free
    buildCommand: |
      echo "=== RENDER BUILD START ==="
      echo "Working directory: $(pwd)"
      echo "Python version: $(python --version)"
      echo "Pip version: $(pip --version)"
      
      echo "=== Installing dependencies ==="
      pip install --upgrade pip
      pip install -r requirements.txt

      echo "=== Preloading embedding model cache ==="
      export HF_HOME=/opt/render/project/src/.cache/huggingface
      mkdir -p "$HF_HOME"
      python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('all-MiniLM-L6-v2'); print('Embedding model cached')"
      
      echo "=== Checking sample data ==="
      ls -lah data/ || echo "data/ directory not found"
      ls -lah data/sample_conversations.jsonl || echo "sample_conversations.jsonl not found"
      
      echo "=== Building FAISS index ==="
      python build_index_production.py || exit 1
      
      echo "=== Verifying index files ==="
      ls -lah data/index.faiss || { echo "ERROR: index.faiss not created!"; exit 1; }
      ls -lah data/metadata.pkl || { echo "ERROR: metadata.pkl not created!"; exit 1; }
      
      echo "=== BUILD SUCCESS ==="
    startCommand: streamlit run app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true --server.enableCORS=false --server.enableXsrfProtection=false
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: GROQ_API_KEY
        sync: false
      - key: HF_HOME
        value: /opt/render/project/src/.cache/huggingface
      - key: ENABLE_RERANKING
        value: false
      - key: QDRANT_TIMEOUT_SECONDS
        value: 20
      - key: PIPELINE_QUERY_TIMEOUT_SECONDS
        value: 90
    healthCheckPath: /_stcore/health
